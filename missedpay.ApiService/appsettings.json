{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "MissedPayDbContext": "Host=localhost;Database=missedpay;Username=postgres;Password=postgres"
  },
  "TenantProvider": {
    "Type": "Header",
    "Description": "Options: Header (X-Tenant-Id header), JWT (tenant_id claim from authenticated user)"
  }
}